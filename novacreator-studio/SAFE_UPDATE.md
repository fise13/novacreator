# ✅ Безопасное обновление через Git

## 🛡️ Гарантии защиты данных

**Ваши данные защищены!** При обновлении через Git:

- ✅ **База данных НЕ удаляется** - все пользователи и прогресс сохраняются
- ✅ **Данные НЕ теряются** - регистрации, проекты, метрики остаются
- ✅ **OAuth работает** - Google OAuth автоматически настроен
- ✅ **Миграции безопасны** - только добавляют новые поля, не удаляют данные

## 🚀 Быстрое обновление (3 команды)

```bash
cd novacreator-studio

# 1. Проверка защиты
php scripts/verify_data_protection.php

# 2. Автоматический бэкап
./scripts/backup_before_update.sh

# 3. Обновление
git pull origin main
```

**Готово!** Данные сохранены, код обновлен.

## 📋 Что защищено

### ✅ База данных
- Файл `data/app.db` в `.gitignore`
- Не отслеживается Git
- Не удаляется при `git pull`
- Не перезаписывается при обновлении

### ✅ Данные пользователей
- Регистрации и профили
- Прогресс проектов (%)
- Время работы (минуты)
- Статусы и этапы
- Заметки и метрики
- OAuth связи

### ✅ OAuth конфигурация
- Google OAuth credentials встроены в код
- Автоматически восстанавливаются
- Можно экспортировать/импортировать

## 🔄 Как работают миграции

Миграции **БЕЗОПАСНЫ** - они только:

- ✅ Создают таблицы, если их нет
- ✅ Добавляют новые колонки, если их нет
- ✅ Создают индексы, если их нет

**НИКОГДА не:**
- ❌ Удаляют таблицы
- ❌ Удаляют колонки
- ❌ Удаляют данные

## 📦 Резервное копирование

### Автоматический бэкап:

```bash
./scripts/backup_before_update.sh
```

Создает:
- `data/backups/app.db.backup.20231206_143000`
- `data/backups/oauth_config_backup.20231206_143000.json`
- Ссылки на последние бэкапы

### Восстановление:

```bash
# Восстановить базу данных
cp data/backups/app.db.latest data/app.db

# Восстановить OAuth
php scripts/import_oauth_config.php data/backups/oauth_config.latest.json
```

## ✅ Проверка после обновления

```bash
# Проверить, что все в порядке
php scripts/verify_data_protection.php
```

Должно показать:
- ✅ База данных существует
- ✅ База данных не отслеживается Git
- ✅ Google OAuth настроен
- ✅ Есть данные пользователей

## 🎯 Итог

**Можно безопасно обновлять код через Git!**

- Данные пользователей защищены
- Прогресс сохраняется
- OAuth работает автоматически
- Бэкапы доступны

Просто выполните `git pull` - все будет работать! 🚀

