# 📱 Настройка отправки форм в Telegram

Этот документ описывает, как настроить отправку всех форм сайта в Telegram через Bot API.

## 📋 Содержание

1. [Быстрый старт](#быстрый-старт)
2. [Получение токена бота](#получение-токена-бота)
3. [Получение Chat ID](#получение-chat-id)
4. [Настройка конфигурации](#настройка-конфигурации)
5. [Подключение новых форм](#подключение-новых-форм)
6. [Защита от спама](#защита-от-спама)
7. [Устранение неполадок](#устранение-неполадок)

---

## 🚀 Быстрый старт

1. **Получите токен бота** (см. раздел ниже)
2. **Получите Chat ID** (см. раздел ниже)
3. **Откройте файл** `backend/telegram_config.php`
4. **Вставьте токен и Chat ID** в соответствующие константы
5. **Готово!** Формы начнут отправляться в Telegram

---

## 🤖 Получение токена бота

1. Откройте Telegram и найдите бота **@BotFather**
2. Отправьте команду `/newbot`
3. Следуйте инструкциям:
   - Введите имя бота (например: "NovaCreator Forms Bot")
   - Введите username бота (должен заканчиваться на `bot`, например: `novacreator_forms_bot`)
4. BotFather выдаст вам токен вида: `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`
5. **Скопируйте токен** и сохраните его в безопасном месте

---

## 💬 Получение Chat ID

### Способ 1: Автоматический (рекомендуется)

1. **Добавьте бота в группу/канал** или начните диалог с ботом
2. **Отправьте любое сообщение** боту (например: "Привет")
3. **Откройте в браузере**: `http://ваш-сайт.com/backend/get_chat_id.php`
4. **Скопируйте Chat ID** из результата
5. **Вставьте Chat ID** в `telegram_config.php`

### Способ 2: Ручной

1. Добавьте бота в группу/канал или начните диалог
2. Отправьте сообщение боту
3. Откройте в браузере:
   ```
   https://api.telegram.org/botВАШ_ТОКЕН/getUpdates
   ```
   Замените `ВАШ_ТОКЕН` на токен вашего бота
4. Найдите в ответе `"chat":{"id":-1001234567890}`
5. Число после `"id":` — это ваш Chat ID

### Типы Chat ID

- **Личный чат**: положительное число (например: `123456789`)
- **Группа**: отрицательное число (например: `-123456789`)
- **Канал**: начинается с `-100` (например: `-1001234567890`)

**Важно для каналов:**
- Добавьте бота как **администратора** канала
- Chat ID канала начинается с `-100`

---

## ⚙️ Настройка конфигурации

Откройте файл `backend/telegram_config.php` и настройте следующие параметры:

```php
// Токен бота (обязательно)
define('TELEGRAM_BOT_TOKEN', 'ваш_токен_здесь');

// Chat ID (обязательно)
define('TELEGRAM_CHAT_ID', 'ваш_chat_id_здесь');

// Минимальное время между отправками в секундах (защита от спама)
define('TELEGRAM_MIN_SEND_INTERVAL', 30);

// Включить логирование (true/false)
define('TELEGRAM_ENABLE_LOGGING', true);
```

### Пример конфигурации

```php
define('TELEGRAM_BOT_TOKEN', '8581188166:AAH2MQ-RYJO2dCOooehOhj_jbLKm7wnkKQo');
define('TELEGRAM_CHAT_ID', '-1001234567890');
define('TELEGRAM_MIN_SEND_INTERVAL', 30);
define('TELEGRAM_ENABLE_LOGGING', true);
```

---

## 📝 Подключение новых форм

Чтобы подключить новую форму к отправке в Telegram:

### Шаг 1: Добавьте класс формы

Добавьте класс `contact-form` к вашей форме:

```html
<form class="contact-form" method="POST" action="/backend/send.php">
    <!-- поля формы -->
</form>
```

### Шаг 2: Добавьте обязательные скрытые поля

```html
<!-- Название формы (будет отображаться в Telegram) -->
<input type="hidden" name="form_name" value="Название вашей формы">

<!-- Honeypot поле для защиты от спама (скрыто) -->
<input type="text" name="website" tabindex="-1" autocomplete="off" 
       style="position: absolute; left: -9999px;" aria-hidden="true">
```

### Шаг 3: Добавьте обязательные поля формы

Форма должна содержать следующие поля:

- `name` — имя (обязательно)
- `email` — email (обязательно, валидация на email)
- `phone` — телефон (обязательно)
- `message` — сообщение (обязательно)

### Шаг 4: Опциональные поля

Вы можете добавить дополнительные поля:

- `service` — услуга
- `type` — тип формы (`contact` или `vacancy`)
- `vacancy` — название вакансии

### Пример полной формы

```html
<form class="contact-form space-y-6" method="POST" action="/backend/send.php">
    <!-- Скрытые поля -->
    <input type="hidden" name="form_name" value="Форма обратной связи">
    <input type="text" name="website" tabindex="-1" autocomplete="off" 
           style="position: absolute; left: -9999px;" aria-hidden="true">
    
    <!-- Обязательные поля -->
    <div>
        <label for="name">Имя <span class="text-red-500">*</span></label>
        <input type="text" id="name" name="name" required>
    </div>
    
    <div>
        <label for="email">Email <span class="text-red-500">*</span></label>
        <input type="email" id="email" name="email" required>
    </div>
    
    <div>
        <label for="phone">Телефон <span class="text-red-500">*</span></label>
        <input type="tel" id="phone" name="phone" required>
    </div>
    
    <div>
        <label for="message">Сообщение <span class="text-red-500">*</span></label>
        <textarea id="message" name="message" required></textarea>
    </div>
    
    <!-- Опциональное поле -->
    <div>
        <label for="service">Услуга</label>
        <select id="service" name="service">
            <option value="">Выберите услугу</option>
            <option value="SEO">SEO</option>
            <option value="Разработка">Разработка</option>
        </select>
    </div>
    
    <button type="submit">Отправить</button>
</form>
```

### Шаг 5: JavaScript обработка

JavaScript автоматически обработает форму, если она имеет класс `contact-form`. 

Форма будет отправляться через `fetch()` API с отображением состояний:
- **"Отправка..."** — во время отправки
- **"Успешно"** — при успешной отправке
- **"Ошибка"** — при ошибке

---

## 🛡️ Защита от спама

Система включает несколько уровней защиты от спама:

### 1. Honeypot поле

Скрытое поле `website`, которое невидимо для пользователей. Если оно заполнено — это бот, заявка блокируется.

### 2. Таймер отправки

Минимальный интервал между отправками (по умолчанию 30 секунд). Настраивается в `telegram_config.php`:

```php
define('TELEGRAM_MIN_SEND_INTERVAL', 30); // секунды
```

### 3. Валидация данных

- Проверка обязательных полей
- Валидация email формата
- Очистка данных от лишних пробелов

---

## 🔧 Устранение неполадок

### Проблема: Сообщения не приходят в Telegram

**Решение:**

1. **Проверьте токен бота:**
   - Убедитесь, что токен правильный в `telegram_config.php`
   - Проверьте, что токен не содержит лишних пробелов

2. **Проверьте Chat ID:**
   - Убедитесь, что Chat ID правильный
   - Для каналов: бот должен быть администратором
   - Для групп: бот должен быть добавлен в группу

3. **Проверьте логи:**
   - Откройте файл `backend/telegram_logs.txt`
   - Найдите ошибки отправки

4. **Проверьте доступность Telegram API:**
   - Убедитесь, что сервер может обращаться к `api.telegram.org`
   - Проверьте настройки firewall

### Проблема: Ошибка "Chat ID not configured"

**Решение:**

1. Установите `TELEGRAM_CHAT_ID` в `telegram_config.php`
2. Или используйте скрипт `get_chat_id.php` для автоматического получения

### Проблема: Форма отправляется, но ничего не происходит

**Решение:**

1. Откройте консоль браузера (F12) и проверьте ошибки
2. Проверьте, что форма имеет класс `contact-form`
3. Проверьте, что путь к `send.php` правильный
4. Проверьте логи в `backend/telegram_logs.txt`

### Проблема: CORS ошибки

**Решение:**

CORS заголовки уже настроены в `send.php`. Если проблема сохраняется:

1. Убедитесь, что форма отправляется на тот же домен
2. Проверьте настройки сервера

### Проблема: Сообщения приходят, но форматирование неправильное

**Решение:**

Telegram использует Markdown форматирование. Убедитесь, что в сообщениях правильно экранированы специальные символы:
- `*` для жирного текста
- `_` для курсива
- `` ` `` для моноширинного текста

---

## 📊 Формат сообщений в Telegram

Сообщения в Telegram имеют следующий формат:

```
🔔 *Новая заявка с сайта*

📋 *Форма:* Название формы

👤 *Имя:* Имя пользователя
📧 *Email:* email@example.com
📱 *Телефон:* +7 123 456 7890
🎯 *Услуга:* SEO-оптимизация

💬 *Сообщение:*
Текст сообщения пользователя

━━━━━━━━━━━━━━━━━━━━
🌐 *IP адрес:* `123.456.789.0`
🕐 *Время:* 2024-01-15 12:30:45
```

---

## 📁 Структура файлов

```
backend/
├── telegram_config.php    # Конфигурация Telegram
├── send.php               # Обработчик форм
├── get_chat_id.php        # Скрипт для получения Chat ID
└── telegram_logs.txt     # Логи отправки (создается автоматически)
```

---

## 🔐 Безопасность

1. **Не публикуйте токен бота** в публичных репозиториях
2. **Добавьте `telegram_config.php` в `.gitignore`** если используете Git
3. **Ограничьте доступ** к файлам конфигурации через `.htaccess`
4. **Регулярно проверяйте логи** на подозрительную активность

---

## 📞 Поддержка

Если у вас возникли проблемы:

1. Проверьте раздел "Устранение неполадок"
2. Проверьте логи в `backend/telegram_logs.txt`
3. Убедитесь, что все настройки выполнены правильно

---

## ✅ Чеклист настройки

- [ ] Получен токен бота от @BotFather
- [ ] Бот добавлен в группу/канал или начат диалог
- [ ] Получен Chat ID через `get_chat_id.php`
- [ ] Токен и Chat ID добавлены в `telegram_config.php`
- [ ] Протестирована отправка формы
- [ ] Проверены логи на наличие ошибок
- [ ] Форма работает на всех страницах сайта

---

**Готово!** Теперь все формы вашего сайта будут отправляться в Telegram. 🎉

