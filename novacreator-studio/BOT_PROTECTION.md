# Защита от ботов - Руководство

Система автоматической защиты от ботов для NovaCreator Studio.

---

## 📋 Содержание

- [Как работает защита](#как-работает-защита)
- [Что блокируется](#что-блокируется)
- [Разрешенные боты](#разрешенные-боты)
- [Логирование](#логирование)
- [Тестирование](#тестирование)
- [Настройка](#настройка)
- [Решение проблем](#решение-проблем)

---

## 🛡️ Как работает защита

Система защиты от ботов работает на уровне PHP и проверяет каждый запрос к сайту:

1. **Проверка User-Agent** - анализирует строку User-Agent на наличие признаков ботов
2. **Проверка браузерных заголовков** - проверяет наличие типичных заголовков браузеров
3. **Поведенческий анализ** - анализирует скорость запросов и паттерны поведения
4. **Проверка Referer** - проверяет подозрительные источники запросов

Если обнаружен бот, запрос блокируется с кодом 403 Forbidden.

---

## 🚫 Что блокируется

### Автоматически блокируются:

- **Скраперы:**
  - curl, wget
  - python-requests, scrapy
  - java, perl, ruby скрипты
  - go-http-client
  - libwww, lwp-trivial, mechanize

- **SEO инструменты** (опционально):
  - SemrushBot
  - AhrefsBot
  - MJ12bot
  - DotBot
  - MegaIndex

- **Подозрительные запросы:**
  - Пустой User-Agent
  - Слишком короткий User-Agent (< 3 символов)
  - User-Agent только из цифр
  - Запросы без браузерных заголовков (Accept, Accept-Language)

- **Поведенческие паттерны:**
  - Слишком быстрые запросы (< 0.1 секунды между запросами)
  - Подозрительные Referer (localhost, 127.0.0.1)

---

## ✅ Разрешенные боты

Следующие боты **не блокируются** (легитимные):

### Поисковые системы:
- Googlebot (Google)
- Bingbot (Bing)
- Yandexbot (Yandex)
- Baiduspider (Baidu)
- Sogou (Sogou)

### Социальные сети:
- FacebookExternalHit (Facebook)
- TwitterBot (Twitter)
- LinkedInBot (LinkedIn)
- SlackBot (Slack)
- WhatsApp
- TelegramBot

### Другие:
- Applebot (Apple)
- Archive.org Bot
- IA Archiver

---

## 📝 Логирование

Все блокировки записываются в файл: `backend/bot_blocked.log`

**Формат лога:**
```
[2025-01-15 12:30:45] BOT BLOCKED - IP: 123.456.789.0 | UA: curl/7.68.0 | URI: /services | Referer: none
```

**Проверка логов:**
```bash
# Просмотр последних блокировок
tail -n 50 backend/bot_blocked.log

# Подсчет блокировок по IP
grep "IP:" backend/bot_blocked.log | awk '{print $4}' | sort | uniq -c | sort -rn

# Поиск конкретного User-Agent
grep "curl" backend/bot_blocked.log
```

**Права доступа:**
Убедитесь, что файл может быть создан:
```bash
chmod 666 backend/bot_blocked.log
# или
touch backend/bot_blocked.log && chmod 666 backend/bot_blocked.log
```

---

## 🧪 Тестирование

### Тест 1: Проверка блокировки curl

```bash
# Этот запрос должен быть заблокирован
curl -A "curl/7.68.0" https://ваш-сайт.com/

# Ожидаемый результат: 403 Forbidden
```

### Тест 2: Проверка блокировки пустого User-Agent

```bash
# Этот запрос должен быть заблокирован
curl -A "" https://ваш-сайт.com/

# Ожидаемый результат: 403 Forbidden
```

### Тест 3: Проверка разрешенного бота

```bash
# Этот запрос должен пройти (Googlebot)
curl -A "Mozilla/5.0 (compatible; Googlebot/2.1; +http://www.google.com/bot.html)" https://ваш-сайт.com/

# Ожидаемый результат: 200 OK (страница загружается)
```

### Тест 4: Проверка обычного браузера

Откройте сайт в обычном браузере (Chrome, Firefox, Safari) - должен работать нормально.

### Тест 5: Проверка логов

После тестов проверьте логи:
```bash
cat backend/bot_blocked.log
```

Должны быть записи о блокировках curl запросов.

---

## ⚙️ Настройка

### Отключение защиты

Если нужно временно отключить защиту, откройте `includes/header.php` и закомментируйте:

```php
// checkAndBlockBots(); // Отключено
```

### Добавление разрешенных ботов

Откройте `includes/bot_detection.php` и добавьте в массив `$allowedBots`:

```php
$allowedBots = [
    // ... существующие боты
    'ваш-бот', // Добавьте здесь
];
```

### Добавление заблокированных паттернов

Откройте `includes/bot_detection.php` и добавьте в массив `$blockedPatterns`:

```php
$blockedPatterns = [
    // ... существующие паттерны
    '/ваш-паттерн/i', // Добавьте здесь
];
```

### Блокировка SEO инструментов

По умолчанию SEO инструменты (Semrush, Ahrefs) **не блокируются**. 

Чтобы заблокировать их, убедитесь, что они не в списке `$allowedBots` и добавьте их в массив `$blockedPatterns` в `includes/bot_detection.php`.

---

## 🐛 Решение проблем

### Проблема: Легитимные пользователи блокируются

**Решение:**
1. Проверьте логи `backend/bot_blocked.log` - какой User-Agent блокируется
2. Добавьте этот User-Agent в список разрешенных в `includes/bot_detection.php`
3. Или временно отключите защиту для отладки

### Проблема: Поисковые системы не индексируют сайт

**Решение:**
1. Проверьте, что Googlebot, Bingbot, Yandexbot в списке разрешенных
2. Проверьте логи - возможно блокируется другой бот
3. Временно отключите защиту для отладки

### Проблема: Логи не создаются

**Решение:**
1. Проверьте права на папку `backend/`:
   ```bash
   chmod 755 backend/
   ```
2. Создайте файл вручную:
   ```bash
   touch backend/bot_blocked.log
   chmod 666 backend/bot_blocked.log
   ```
3. Проверьте, что PHP может писать в эту папку

### Проблема: Админ-панель блокируется

**Решение:**
Система автоматически пропускает запросы к `/admin` и `/backend`. Если проблема сохраняется:

1. Проверьте, что путь правильный в `includes/bot_detection.php`:
   ```php
   if (strpos($requestUri, '/admin') === 0 || strpos($requestUri, '/backend') === 0) {
       return; // Пропускаем проверку
   }
   ```
2. Временно отключите защиту для отладки

### Проблема: Слишком много ложных срабатываний

**Решение:**
1. Проверьте логи - какие User-Agent блокируются
2. Добавьте их в список разрешенных
3. Или ослабьте проверки в функции `hasBrowserHeaders()` и `hasBotBehavior()`

---

## 📊 Мониторинг

### Регулярная проверка логов

Рекомендуется регулярно проверять логи для анализа атак:

```bash
# Ежедневная проверка
grep "$(date +%Y-%m-%d)" backend/bot_blocked.log | wc -l

# Топ заблокированных IP
grep "IP:" backend/bot_blocked.log | awk '{print $4}' | sort | uniq -c | sort -rn | head -10

# Топ заблокированных User-Agent
grep "UA:" backend/bot_blocked.log | awk -F'|' '{print $2}' | sort | uniq -c | sort -rn | head -10
```


---

## 🔗 Связанные файлы

- `includes/bot_detection.php` - основной файл защиты
- `includes/header.php` - интеграция защиты
- `backend/bot_blocked.log` - файл логов

---

**Версия:** 1.0.0  
**Последнее обновление:** Ноябрь 2025

