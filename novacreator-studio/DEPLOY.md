# Инструкция по деплою на сервер

## 🔧 Требования к серверу

### Обязательные требования:
1. **PHP 7.4 или выше** (рекомендуется PHP 8.0+)
2. **Apache** с поддержкой mod_rewrite или **Nginx**
3. **Node.js 16+ и npm** (для компиляции CSS)
4. **Права на запись** для папок `data/` и `backend/`

### Опционально (для оптимизации):
- SSL сертификат (HTTPS)
- GZIP сжатие
- Кэширование

---

## 📦 Шаг 1: Подготовка файлов локально

### 1.1. Компиляция CSS

**ВАЖНО:** Перед загрузкой на сервер необходимо скомпилировать CSS файл!

```bash
# Перейдите в папку проекта
cd novacreator-studio

# Установите зависимости (если еще не установлены)
npm install

# Скомпилируйте CSS для продакшена (минифицированный)
npm run build-css-prod
```

После выполнения команды файл `assets/css/output.css` будет обновлен и готов к загрузке.

---

## 🚀 Шаг 2: Загрузка файлов на сервер

### 2.1. Что загружать

Загрузите **ВСЁ содержимое** папки `novacreator-studio/` на сервер в корневую директорию сайта:
- Обычно это `public_html/`, `httpdocs/`, `www/` или `htdocs/`

### 2.2. Структура на сервере должна быть:

```
/
├── index.php
├── services.php
├── seo.php
├── ads.php
├── ... (все PHP файлы)
├── assets/
│   ├── css/
│   │   ├── input.css
│   │   └── output.css  ← ВАЖНО: должен быть скомпилирован!
│   ├── js/
│   │   └── main.js
│   └── img/
│       └── logo.svg
├── backend/
│   ├── send.php
│   └── requests.txt
├── data/
│   └── blog.json
├── includes/
├── admin/
├── telegram_bot/
├── favicon.ico
├── robots.txt
├── sitemap.xml
└── .htaccess
```

---

## ⚙️ Шаг 3: Настройка прав доступа

Выполните через SSH или FTP клиент:

```bash
# Права на папки (755 = чтение и выполнение для всех, запись для владельца)
chmod 755 data/
chmod 755 backend/
chmod 755 assets/
chmod 755 assets/css/
chmod 755 assets/js/
chmod 755 assets/img/

# Права на файлы для записи (666 = чтение и запись для всех)
chmod 666 data/blog.json
chmod 666 backend/requests.txt

# Права на .htaccess (если используется Apache)
chmod 644 .htaccess
```

---

## 🔐 Шаг 4: Настройка конфигурации

### 4.1. Telegram бот

1. Скопируйте `telegram_bot/config.example.php` в `telegram_bot/config.php`
2. Откройте `telegram_bot/config.php` и заполните:
   ```php
   define('TELEGRAM_BOT_TOKEN', 'ваш_токен_бота');
   define('TELEGRAM_CHAT_ID', 'ваш_chat_id');
   ```

### 4.2. Админ-панель

1. Откройте `admin/config.php`
2. Измените пароль:
   ```php
   define('ADMIN_PASSWORD', 'ваш_надежный_пароль');
   ```
3. Также обновите пароль в `admin/login.php` (строка с проверкой)

### 4.3. Обновление домена (если нужно)

Если нужно изменить базовый URL сайта, откройте `includes/seo_meta.php` и найдите:
```php
$siteUrl = 'https://novacreator-studio.com';
```
Измените на ваш домен.

---

## 🌐 Шаг 5: Настройка веб-сервера

### Apache

1. Убедитесь, что включен `mod_rewrite`:
   ```bash
   # Проверка (на сервере)
   apache2ctl -M | grep rewrite
   # или
   httpd -M | grep rewrite
   ```

2. Файл `.htaccess` должен быть в корне сайта

3. Если сайт в подпапке (например, `/novacreator-studio/`), обновите `RewriteBase` в `.htaccess`:
   ```apache
   RewriteBase /novacreator-studio/
   ```

### Nginx

Если используете Nginx, добавьте в конфигурацию:

```nginx
location / {
    try_files $uri $uri/ /index.php?$query_string;
}

location ~ \.php$ {
    fastcgi_pass unix:/var/run/php/php8.0-fpm.sock;
    fastcgi_index index.php;
    fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
    include fastcgi_params;
}

# Защита конфигурационных файлов
location ~ /\.(ht|git|env) {
    deny all;
}
```

---

## ✅ Шаг 6: Проверка работы

### 6.1. Проверьте основные страницы:

- ✅ `https://ваш-домен.com/` - главная страница
- ✅ `https://ваш-домен.com/services.php` - услуги
- ✅ `https://ваш-домен.com/contact.php` - контакты
- ✅ `https://ваш-домен.com/admin/login.php` - админ-панель

### 6.2. Проверьте загрузку CSS:

Откройте консоль браузера (F12) и проверьте:
- Нет ли ошибок 404 для `assets/css/output.css`
- CSS файл загружается и применяется

### 6.3. Проверьте работу форм:

- Отправьте тестовую заявку через форму контактов
- Проверьте, что данные сохраняются в `backend/requests.txt`
- Проверьте отправку в Telegram (если настроено)

### 6.4. Проверьте админ-панель:

- Войдите в `/admin/login.php`
- Создайте тестовую статью в блоге
- Проверьте, что она отображается на `/blog.php`

---

## 🐛 Решение проблем

### Проблема: CSS не загружается / сайт без стилей

**Решение:**
1. Убедитесь, что файл `assets/css/output.css` существует и не пустой
2. Проверьте права доступа: `chmod 644 assets/css/output.css`
3. Проверьте путь в `includes/header.php` (строка 34):
   ```php
   <link href="./assets/css/output.css" rel="stylesheet">
   ```
   Если сайт в подпапке, путь должен быть: `/подпапка/assets/css/output.css`
4. Перекомпилируйте CSS локально и загрузите заново:
   ```bash
   npm run build-css-prod
   ```

### Проблема: Формы не отправляются

**Решение:**
1. Проверьте права на `backend/requests.txt`: `chmod 666 backend/requests.txt`
2. Проверьте права на папку `backend/`: `chmod 755 backend/`
3. Проверьте логи ошибок PHP на сервере
4. Убедитесь, что PHP функция `mail()` работает (для отправки email)

### Проблема: 404 ошибки на всех страницах

**Решение:**
1. Проверьте, что `.htaccess` загружен на сервер
2. Убедитесь, что `mod_rewrite` включен в Apache
3. Проверьте `RewriteBase` в `.htaccess` (если сайт в подпапке)

### Проблема: Админ-панель не работает

**Решение:**
1. Проверьте права на `data/blog.json`: `chmod 666 data/blog.json`
2. Проверьте права на папку `data/`: `chmod 755 data/`
3. Проверьте, что файл `data/blog.json` существует (если нет, создайте пустой файл: `{}`)

### Проблема: Telegram бот не отправляет сообщения

**Решение:**
1. Проверьте, что `telegram_bot/config.php` существует и заполнен
2. Проверьте токен бота через [@BotFather](https://t.me/BotFather)
3. Проверьте Chat ID (начните диалог с ботом)
4. Проверьте логи ошибок PHP

---

## 📝 Чек-лист перед запуском

- [ ] CSS скомпилирован (`npm run build-css-prod`)
- [ ] Все файлы загружены на сервер
- [ ] Права доступа установлены правильно
- [ ] `telegram_bot/config.php` настроен
- [ ] Пароль админ-панели изменен
- [ ] `.htaccess` загружен и работает
- [ ] Домен обновлен в `includes/seo_meta.php` (если нужно)
- [ ] Проверена работа форм
- [ ] Проверена работа админ-панели
- [ ] Проверена отправка в Telegram

---

## 🔄 Обновление сайта

При обновлении сайта:

1. **Компилируйте CSS заново:**
   ```bash
   npm run build-css-prod
   ```

2. **Загрузите обновленные файлы** на сервер

3. **Проверьте работу** после обновления

---

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи ошибок на сервере
2. Проверьте консоль браузера (F12) на наличие ошибок
3. Убедитесь, что все требования выполнены

---

**Важно:** Всегда компилируйте CSS перед загрузкой на сервер! Без скомпилированного `output.css` сайт будет отображаться без стилей.

