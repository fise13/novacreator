# 🚨 Быстрое решение: Сайт без стилей

## Проблема
Сайт отображается без стилей (белый фон, нет оформления).

## Причина
CSS файл не скомпилирован или не загружен на сервер.

## Решение (5 минут)

### 1. Локально на вашем компьютере:

```bash
# Перейдите в папку проекта
cd novacreator-studio

# Установите зависимости (если еще не установлены)
npm install

# Скомпилируйте CSS
npm run build-css-prod
```

### 2. Загрузите файл на сервер:

Загрузите файл `assets/css/output.css` на сервер в папку `assets/css/`

**Важно:** Файл должен быть загружен по пути: `ваш-сайт.com/assets/css/output.css`

### 3. Проверьте права доступа:

```bash
chmod 644 assets/css/output.css
```

### 4. Проверьте в браузере:

Откройте сайт и нажмите `Ctrl+F5` (или `Cmd+Shift+R` на Mac) для обновления кэша.

---

## Если проблема осталась

### Проверьте путь к CSS:

Откройте `includes/header.php` и проверьте строку 34:

```php
<link href="./assets/css/output.css" rel="stylesheet">
```

Если сайт находится в подпапке (например, `/novacreator-studio/`), измените на:

```php
<link href="/novacreator-studio/assets/css/output.css" rel="stylesheet">
```

Или используйте абсолютный путь:

```php
<link href="<?php echo $_SERVER['REQUEST_SCHEME'] . '://' . $_SERVER['HTTP_HOST']; ?>/assets/css/output.css" rel="stylesheet">
```

---

## Альтернатива: Компиляция на сервере

Если у вас есть доступ к SSH и Node.js на сервере:

```bash
# На сервере
cd /путь/к/сайту/novacreator-studio
npm install
npm run build-css-prod
```

---

## Проверка

Откройте в браузере: `https://ваш-сайт.com/assets/css/output.css`

Если файл открывается и показывает CSS код - путь правильный.
Если ошибка 404 - проверьте путь и загрузите файл заново.

