# 🚀 Быстрая установка Google OAuth на сервере

## Самый простой способ (1 минута):

### 1. Откройте файл `.htaccess` на сервере

### 2. Найдите в конце файла секцию:

```apache
# ===================================
# OAUTH КОНФИГУРАЦИЯ (GOOGLE, APPLE)
# ===================================
```

### 3. Раскомментируйте и вставьте ваши данные:

```apache
SetEnv GOOGLE_CLIENT_ID "394392440430-atshc1pnu69bbgal894ob1rs15phovjo.apps.googleusercontent.com"
SetEnv GOOGLE_CLIENT_SECRET "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"
```

### 4. Сохраните файл

**Готово!** Система автоматически сохранит эти данные в базу данных при первом запросе к сайту.

## Проверка:

Откройте в браузере: `https://novacreatorstudio.com/oauth/google/test.php`

Должно показать: ✅ OAuth включен: ДА

## Альтернативные способы:

### Через скрипт:

```bash
# Установите переменные окружения в .htaccess, затем:
php scripts/install_oauth_from_env.php
```

### Через импорт резервной копии:

```bash
php scripts/import_oauth_config.php data/backups/oauth_config_backup.json
```

## Что происходит автоматически:

✅ При каждом подключении к БД система проверяет переменные окружения  
✅ Если они установлены - автоматически сохраняет в БД  
✅ Если в БД пусто - заполняет из переменных окружения  
✅ Если переменные изменились - обновляет БД  

**Результат:** Google OAuth всегда будет работать на сервере! 🎉

