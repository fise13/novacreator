# Инструкция по подключению проекта к Cloudflare

Подробное руководство по настройке Cloudflare для PHP сайта NovaCreator Studio.

---

## 📋 Содержание

- [Преимущества Cloudflare](#преимущества-cloudflare)
- [Шаг 1: Регистрация и добавление сайта](#шаг-1-регистрация-и-добавление-сайта)
- [Шаг 2: Настройка DNS записей](#шаг-2-настройка-dns-записей)
- [Шаг 3: Настройка SSL/TLS](#шаг-3-настройка-ssltls)
- [Шаг 4: Настройка кэширования](#шаг-4-настройка-кэширования)
- [Шаг 5: Оптимизация производительности](#шаг-5-оптимизация-производительности)
- [Шаг 6: Настройка безопасности](#шаг-6-настройка-безопасности)
- [Шаг 7: Настройка для PHP сайта](#шаг-7-настройка-для-php-сайта)
- [Проверка работы](#проверка-работы)
- [Решение проблем](#решение-проблем)

---

## 🎯 Преимущества Cloudflare

После подключения вы получите:

- ✅ **Бесплатный SSL сертификат** (автоматическое обновление)
- ✅ **CDN** - ускорение загрузки сайта по всему миру
- ✅ **Защита от DDoS** атак
- ✅ **Кэширование статики** - уменьшение нагрузки на сервер
- ✅ **Оптимизация изображений** (в платных планах)
- ✅ **Аналитика трафика**
- ✅ **Firewall правила** для защиты от ботов и спама

---

## 🚀 Шаг 1: Регистрация и добавление сайта

### 1.1. Регистрация в Cloudflare

1. Перейдите на [cloudflare.com](https://www.cloudflare.com)
2. Нажмите **"Sign Up"** и создайте аккаунт
3. Подтвердите email

### 1.2. Добавление сайта

1. В панели Cloudflare нажмите **"Add a Site"**
2. Введите ваш домен (например: `novacreatorstudio.com`)
3. Нажмите **"Add site"**
4. Выберите **бесплатный план** (Free) и нажмите **"Continue"**

### 1.3. Сканирование DNS записей

Cloudflare автоматически сканирует ваши текущие DNS записи. Проверьте, что все записи обнаружены правильно.

---

## 🌐 Шаг 2: Настройка DNS записей

### 2.1. Основные DNS записи

Вам нужно настроить следующие записи:

#### **A запись (IPv4)**
```
Тип: A
Имя: @ (или ваш домен)
IPv4 адрес: [IP адрес вашего сервера]
Прокси: 🟠 Proxied (включено)
TTL: Auto
```

#### **A запись для www**
```
Тип: A
Имя: www
IPv4 адрес: [IP адрес вашего сервера]
Прокси: 🟠 Proxied (включено)
TTL: Auto
```

#### **CNAME для поддоменов (если нужны)**
```
Тип: CNAME
Имя: mail (или другой поддомен)
Цель: mail.yourhosting.com
Прокси: ⚪ DNS only (выключено)
TTL: Auto
```

### 2.2. Важные моменты

- ✅ **Прокси должен быть включен** (🟠 оранжевое облако) для основных записей - это активирует CDN и защиту
- ⚪ **Прокси выключен** (серое облако) для почты и других сервисов, которые не должны проходить через Cloudflare
- После добавления записей нажмите **"Continue"**

### 2.3. Изменение nameservers

Cloudflare предоставит вам два nameserver адреса, например:
```
ns1.cloudflare.com
ns2.cloudflare.com
```

**ВАЖНО:** Нужно изменить nameservers у вашего регистратора домена:

1. Войдите в панель управления доменом (у регистратора)
2. Найдите раздел **"Nameservers"** или **"DNS серверы"**
3. Замените текущие nameservers на те, что предоставил Cloudflare
4. Сохраните изменения

**Время распространения:** Обычно 24-48 часов, но может быть быстрее (от 15 минут до нескольких часов).

---

## 🔒 Шаг 3: Настройка SSL/TLS

### 3.1. Режим SSL/TLS

Перейдите в **SSL/TLS** → **Overview**:

**Рекомендуемая настройка:**
```
Режим: Full (strict)
```

**Варианты режимов:**
- **Off** - SSL отключен (не рекомендуется)
- **Flexible** - SSL между браузером и Cloudflare, но не между Cloudflare и сервером
- **Full** - SSL везде, но Cloudflare не проверяет сертификат сервера
- **Full (strict)** - SSL везде + проверка сертификата сервера (требует валидный SSL на сервере)

### 3.2. Автоматический HTTPS Rewrites

Перейдите в **SSL/TLS** → **Edge Certificates**:

Включите:
- ✅ **Always Use HTTPS** - автоматический редирект HTTP → HTTPS
- ✅ **Automatic HTTPS Rewrites** - автоматическая замена HTTP ссылок на HTTPS

### 3.3. Минимальная версия TLS

Рекомендуется:
```
Минимальная версия TLS: 1.2
```

---

## ⚡ Шаг 4: Настройка кэширования

### 4.1. Уровни кэширования

Перейдите в **Caching** → **Configuration**:

**Рекомендуемые настройки:**

```
Caching Level: Standard
Browser Cache TTL: Respect Existing Headers
```

### 4.2. Правила кэширования (Page Rules)

Перейдите в **Rules** → **Page Rules** и создайте правила:

#### **Правило 1: Кэширование статики**
```
URL Pattern: *novacreatorstudio.com/assets/*
Settings:
  - Cache Level: Cache Everything
  - Edge Cache TTL: 1 month
```

#### **Правило 2: НЕ кэшировать PHP файлы**
```
URL Pattern: *novacreatorstudio.com/*.php
Settings:
  - Cache Level: Bypass
```

#### **Правило 3: НЕ кэшировать админ-панель**
```
URL Pattern: *novacreatorstudio.com/admin/*
Settings:
  - Cache Level: Bypass
```

#### **Правило 4: НЕ кэшировать backend**
```
URL Pattern: *novacreatorstudio.com/backend/*
Settings:
  - Cache Level: Bypass
```

**Примечание:** На бесплатном плане доступно только 3 правила. Выберите самые важные.

### 4.3. Очистка кэша

После обновления сайта очистите кэш:
1. Перейдите в **Caching** → **Purge Cache**
2. Нажмите **"Purge Everything"** или выберите конкретные URL

---

## 🚀 Шаг 5: Оптимизация производительности

### 5.1. Auto Minify

Перейдите в **Speed** → **Optimization**:

Включите минификацию:
- ✅ **JavaScript**
- ✅ **CSS**
- ✅ **HTML**

**Примечание:** Убедитесь, что ваш `main.js` уже минифицирован (`main.min.js`), иначе могут быть конфликты.

### 5.2. Brotli

Включите:
- ✅ **Brotli** - улучшенное сжатие (если доступно на вашем плане)

### 5.3. Rocket Loader

**⚠️ ВНИМАНИЕ:** Rocket Loader может конфликтовать с некоторыми JavaScript библиотеками.

Рекомендуется:
- ⚪ **Rocket Loader: Off** (по умолчанию)

Если хотите попробовать:
- ✅ Включите и протестируйте работу сайта

---

## 🛡️ Шаг 6: Настройка безопасности

### 6.1. Security Level

Перейдите в **Security** → **Settings**:

**Рекомендуемая настройка:**
```
Security Level: Medium
```

**Варианты:**
- **Essentially Off** - минимальная защита
- **Low** - базовая защита
- **Medium** - рекомендуемый уровень
- **High** - строгая защита (может блокировать легитимных пользователей)
- **I'm Under Attack!** - максимальная защита (только при атаке)

### 6.2. Challenge Passage

```
Challenge Passage: 30 minutes
```

### 6.3. Firewall Rules

Перейдите в **Security** → **WAF** → **Custom Rules**:

#### **Правило 1: Блокировка вредоносных ботов**
```
Rule name: Block Bad Bots
Expression: (http.user_agent contains "bot" and not http.user_agent contains "googlebot" and not http.user_agent contains "bingbot" and not http.user_agent contains "yandexbot" and not http.user_agent contains "facebookexternalhit" and not http.user_agent contains "twitterbot" and not http.user_agent contains "linkedinbot" and not http.user_agent contains "applebot")
Action: Block
```

#### **Правило 2: Блокировка скраперов и автоматизированных инструментов**
```
Rule name: Block Scrapers
Expression: (http.user_agent contains "curl" or http.user_agent contains "wget" or http.user_agent contains "python" or http.user_agent contains "scrapy" or http.user_agent contains "scraper" or http.user_agent contains "crawler" or http.user_agent contains "spider")
Action: Block
```

#### **Правило 3: Блокировка пустых User-Agent**
```
Rule name: Block Empty User Agent
Expression: (not http.user_agent exists or http.user_agent eq "")
Action: Block
```

#### **Правило 4: Блокировка SEO-скраперов (опционально)**
Если хотите заблокировать SEO инструменты (Semrush, Ahrefs и т.д.):
```
Rule name: Block SEO Scrapers
Expression: (http.user_agent contains "semrush" or http.user_agent contains "ahrefs" or http.user_agent contains "mj12bot" or http.user_agent contains "dotbot" or http.user_agent contains "megaindex")
Action: Block
```

**Примечание:** Если вам нужны данные от SEO инструментов, не используйте правило 4.

#### **Правило 5: Защита админ-панели**
```
Rule name: Protect Admin Panel
Expression: (http.request.uri.path contains "/admin" and ip.geoip.country ne "KZ")
Action: Challenge (CAPTCHA)
```

**Примечание:** Настройте под ваши нужды. На бесплатном плане доступны базовые правила (до 5 правил).

### 6.4. Rate Limiting

На бесплатном плане ограничено, но можно настроить базовую защиту от спама:

Перейдите в **Security** → **WAF** → **Rate limiting rules**:

```
Rule name: Limit Form Submissions
Match: (http.request.uri.path eq "/backend/send.php")
Requests: 5
Period: 1 minute
Action: Block
```

---

## 🤖 Шаг 7: Защита от ботов

### 7.1. PHP система защиты от ботов

Проект включает встроенную систему защиты от ботов на уровне PHP:

**Файл:** `includes/bot_detection.php`

**Что проверяет:**
- ✅ User-Agent строки (блокирует известных ботов и скраперов)
- ✅ Отсутствие браузерных заголовков (Accept, Accept-Language и т.д.)
- ✅ Поведенческие паттерны (скорость запросов)
- ✅ Подозрительные Referer заголовки

**Разрешенные боты:**
- Googlebot, Bingbot, Yandexbot (поисковые системы)
- Facebook, Twitter, LinkedIn боты (социальные сети)
- Другие легитимные боты

**Автоматически блокирует:**
- Скраперы (curl, wget, python, scrapy)
- SEO инструменты (Semrush, Ahrefs и т.д.) - опционально
- Боты без User-Agent или с подозрительными User-Agent
- Запросы без браузерных заголовков

**Логирование:**
Все блокировки записываются в `backend/bot_blocked.log`

### 7.2. Комбинированная защита

Рекомендуется использовать **двухуровневую защиту**:

1. **Cloudflare уровень** - блокирует на уровне CDN (быстрее, меньше нагрузки на сервер)
2. **PHP уровень** - дополнительная проверка на сервере (более гибкая)

Оба уровня работают вместе для максимальной защиты.

## 🔧 Шаг 8: Настройка для PHP сайта

### 8.1. Настройка .htaccess для Cloudflare

Ваш текущий `.htaccess` уже настроен правильно. Убедитесь, что он содержит:

```apache
# Разрешаем CORS для backend файлов
<FilesMatch "^backend/.*\.php$">
    <IfModule mod_headers.c>
        Header set Access-Control-Allow-Origin "*"
        Header set Access-Control-Allow-Methods "POST, GET, OPTIONS"
        Header set Access-Control-Allow-Headers "Content-Type, X-Requested-With"
    </IfModule>
</FilesMatch>
```

### 8.2. Получение реального IP адреса пользователя

Cloudflare проксирует запросы, поэтому нужно получать реальный IP из заголовков Cloudflare.

Создайте файл `includes/cloudflare_ip.php`:

```php
<?php
/**
 * Получение реального IP адреса пользователя через Cloudflare
 */
function getRealIP() {
    // Проверяем заголовки Cloudflare
    if (isset($_SERVER['HTTP_CF_CONNECTING_IP'])) {
        return $_SERVER['HTTP_CF_CONNECTING_IP'];
    }
    
    // Fallback на стандартные методы
    if (isset($_SERVER['HTTP_X_FORWARDED_FOR'])) {
        $ips = explode(',', $_SERVER['HTTP_X_FORWARDED_FOR']);
        return trim($ips[0]);
    }
    
    if (isset($_SERVER['HTTP_X_REAL_IP'])) {
        return $_SERVER['HTTP_X_REAL_IP'];
    }
    
    return $_SERVER['REMOTE_ADDR'] ?? '0.0.0.0';
}
```

Затем в `backend/send.php` замените получение IP:

```php
// Старый код:
// $ip = $_SERVER['REMOTE_ADDR'] ?? '0.0.0.0';

// Новый код:
require_once __DIR__ . '/../includes/cloudflare_ip.php';
$ip = getRealIP();
```

### 8.3. Настройка для форм

Убедитесь, что формы работают через Cloudflare:

1. Проверьте, что `backend/send.php` принимает POST запросы
2. Убедитесь, что CORS настроен правильно (уже есть в `.htaccess`)
3. Протестируйте отправку формы после подключения

### 8.4. Настройка сессий

Если используете PHP сессии, убедитесь, что они работают через Cloudflare:

В `includes/header.php` или в начале каждого PHP файла:

```php
// Убедитесь, что сессии работают через Cloudflare
if (session_status() === PHP_SESSION_NONE) {
    session_start();
}
```

---

## ✅ Проверка работы

### 1. Проверка DNS

После изменения nameservers проверьте:

```bash
# Проверка nameservers
nslookup -type=NS novacreatorstudio.com

# Должны вернуться nameservers Cloudflare
```

### 2. Проверка SSL

1. Откройте сайт в браузере: `https://novacreatorstudio.com`
2. Проверьте, что есть зеленый замочек 🔒
3. Проверьте сертификат (кликните на замочек)

### 3. Проверка CDN

Проверьте заголовки ответа:

```bash
curl -I https://novacreatorstudio.com

# Должны быть заголовки:
# CF-Cache-Status: ...
# CF-Ray: ...
# Server: cloudflare
```

### 4. Проверка работы сайта

- ✅ Главная страница загружается
- ✅ CSS и JS файлы загружаются
- ✅ Формы отправляются
- ✅ Админ-панель работает
- ✅ Блог работает

### 5. Проверка скорости

Используйте инструменты:
- [PageSpeed Insights](https://pagespeed.web.dev/)
- [GTmetrix](https://gtmetrix.com/)
- [WebPageTest](https://www.webpagetest.org/)

---

## 🐛 Решение проблем

### Проблема: Сайт не загружается после подключения

**Решение:**
1. Проверьте, что nameservers изменены правильно
2. Подождите до 48 часов для полного распространения DNS
3. Проверьте, что IP адрес сервера правильный в DNS записях
4. Убедитесь, что прокси включен (🟠 оранжевое облако)

### Проблема: Формы не отправляются

**Решение:**
1. Проверьте, что `backend/send.php` не кэшируется (Page Rule: Bypass)
2. Проверьте CORS настройки в `.htaccess`
3. Проверьте Firewall правила - возможно блокируются запросы
4. Временно отключите Security Level или добавьте исключение

### Проблема: Админ-панель не работает

**Решение:**
1. Убедитесь, что `/admin/*` не кэшируется (Page Rule: Bypass)
2. Проверьте, что сессии работают правильно
3. Проверьте Firewall правила - возможно блокируется доступ

### Проблема: SSL ошибки

**Решение:**
1. Убедитесь, что на сервере установлен SSL сертификат (для Full strict режима)
2. Попробуйте режим **Full** вместо **Full (strict)**
3. Проверьте, что сертификат не истек

### Проблема: Старые версии файлов отображаются

**Решение:**
1. Очистите кэш Cloudflare: **Caching** → **Purge Cache** → **Purge Everything**
2. Очистите кэш браузера (Ctrl+Shift+Delete)
3. Проверьте Page Rules - возможно файлы кэшируются неправильно

### Проблема: Слишком много блокировок (false positives)

**Решение:**
1. Понизьте Security Level до **Low** или **Medium**
2. Добавьте исключения в Firewall для вашего IP
3. Отключите Challenge Passage или уменьшите время

---

## 📝 Чек-лист подключения

- [ ] Зарегистрирован аккаунт в Cloudflare
- [ ] Добавлен сайт в Cloudflare
- [ ] Настроены DNS записи (A записи с прокси)
- [ ] Изменены nameservers у регистратора
- [ ] Настроен SSL/TLS режим (Full или Full strict)
- [ ] Включен "Always Use HTTPS"
- [ ] Настроены Page Rules для кэширования
- [ ] Настроен Security Level
- [ ] Создан файл `includes/cloudflare_ip.php` (если нужен)
- [ ] Обновлен код получения IP в `backend/send.php`
- [ ] Протестирована работа сайта
- [ ] Протестирована отправка форм
- [ ] Протестирована админ-панель
- [ ] Очищен кэш после настройки

---

## 🔗 Полезные ссылки

- [Документация Cloudflare](https://developers.cloudflare.com/)
- [Cloudflare Community](https://community.cloudflare.com/)
- [Проверка DNS](https://www.whatsmydns.net/)
- [Проверка SSL](https://www.ssllabs.com/ssltest/)

---

## 📞 Поддержка

Если возникли проблемы:

1. Проверьте документацию Cloudflare
2. Обратитесь в поддержку Cloudflare (бесплатный план имеет ограниченную поддержку)
3. Проверьте логи на сервере
4. Проверьте консоль браузера (F12) на наличие ошибок

---

**Важно:** После подключения к Cloudflare всегда очищайте кэш при обновлении сайта, особенно при изменении CSS/JS файлов.

**Версия:** 1.0.0  
**Последнее обновление:** Ноябрь 2025

