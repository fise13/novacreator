# План рефакторинга проекта NovaCreator Studio

## ✅ Выполнено

### 1. Создана структура компонентов
- Создана директория `includes/partials/` для переиспользуемых компонентов
- Созданы базовые компоненты:
  - `hero-section.php` - Hero секции
  - `section-header.php` - Заголовки секций
  - `cta-section.php` - CTA секции
  - `service-card.php` - Карточки услуг
  - `content-section.php` - Контентные секции

### 2. Применение компонентов (примеры)
- Главная страница (`index.php`): применены hero и карточки услуг
- Страница услуг (`services.php`): применена CTA секция

## 📋 Следующие шаги

### Шаг 1: Применить компоненты на всех страницах
Применить созданные компоненты на остальных страницах:

**Страницы для обновления:**
- `seo.php` - применить hero, section-header, cta
- `ads.php` - применить hero, section-header, cta  
- `about.php` - применить hero, section-header
- `contact.php` - применить hero, section-header
- `faq.php` - применить hero, section-header
- `blog.php` - применить hero, section-header
- `vacancies.php` - применить hero, section-header
- `landing-page-development.php` - применить hero, section-header, cta
- `ecommerce-development.php` - применить hero, section-header, cta
- `corporate-website-development.php` - применить hero, section-header, cta

### Шаг 2: Унификация отступов и типографики
Создать единые константы для:
- Отступов между секциями: `py-16 md:py-20 lg:py-32`
- Размеров заголовков:
  - H1: `text-5xl sm:text-6xl md:text-7xl lg:text-8xl xl:text-9xl 2xl:text-[10rem]`
  - H2: `text-3xl sm:text-4xl md:text-5xl lg:text-6xl xl:text-7xl 2xl:text-8xl`
  - H3: `text-2xl sm:text-3xl md:text-4xl`
- Шрифтов и весов

### Шаг 3: Реорганизация структуры файлов
Группировка страниц по логике:

```
novacreator-studio/
├── core/                    # Основные страницы
│   ├── index.php           # Главная
│   ├── services.php        # Услуги
│   └── contact.php         # Контакты
├── services/               # Страницы услуг
│   ├── seo.php
│   ├── ads.php
│   ├── landing-page-development.php
│   ├── ecommerce-development.php
│   └── corporate-website-development.php
└── info/                   # Информационные страницы
    ├── about.php
    ├── faq.php
    ├── blog.php
    └── vacancies.php
```

**Важно:** После перемещения файлов нужно будет обновить:
- `.htaccess` для правильной маршрутизации
- Ссылки в навигации
- Breadcrumbs
- Sitemap

### Шаг 4: Приведение имен файлов к kebab-case
Переименовать файлы в единый стиль:
- `seo.php` → `seo.php` (уже kebab-case)
- `ads.php` → `ads.php` (уже kebab-case)
- `landing-page-development.php` → уже kebab-case ✅
- `ecommerce-development.php` → уже kebab-case ✅
- `corporate-website-development.php` → уже kebab-case ✅
- `blog.php` → `blog.php` ✅
- `faq.php` → `faq.php` ✅
- `about.php` → `about.php` ✅

Большинство файлов уже в kebab-case, нужно только проверить.

### Шаг 5: Создание единых стилей для карточек
Вынести стили карточек в отдельный файл или использовать единые классы:
- Карточки услуг
- Карточки проектов
- Карточки преимуществ
- Карточки этапов работы

Все должны иметь единый стиль:
- Одинаковые отступы: `p-8 md:p-10`
- Одинаковый border-radius: `rounded-2xl`
- Одинаковые hover-эффекты: `hover:scale-[1.02] hover:shadow-2xl`
- Одинаковые переходы: `transition-all duration-500`

### Шаг 6: Проверка мобильной адаптивности
Проверить все страницы на:
- Правильные отступы на мобильных
- Читаемость текста (минимальный размер 16px)
- Достаточный размер кнопок (min-height: 44px)
- Правильное отображение форм
- Touch-friendly элементы

### Шаг 7: Очистка CSS
- Удалить дублирующиеся стили
- Объединить повторяющиеся классы
- Проверить неиспользуемые стили
- Упорядочить `input.css`

## Рекомендации

1. **Постепенное внедрение**: Не переделывать всё сразу, делать по страницам
2. **Тестирование**: После каждого изменения проверять работу на разных устройствах
3. **Обратная совместимость**: Не ломать существующие функции
4. **Документация**: Обновлять README при изменениях

## Примеры использования компонентов

### Hero секция
```php
$heroTitle = 'Заголовок';
$heroSubtitle = 'Подзаголовок';
$heroCtaButtons = [
    ['text' => 'Кнопка', 'url' => '/link']
];
include __DIR__ . '/includes/partials/hero-section.php';
```

### Заголовок секции
```php
$sectionTitle = 'Заголовок';
$sectionSubtitle = 'Описание';
include __DIR__ . '/includes/partials/section-header.php';
```

### CTA секция
```php
$ctaTitle = 'Готовы начать?';
$ctaButtonText = 'Связаться';
$ctaButtonUrl = '/contact';
include __DIR__ . '/includes/partials/cta-section.php';
```

### Карточка услуги
```php
$cardTitle = 'Название';
$cardDescription = 'Описание';
$cardLinkUrl = '/link';
include __DIR__ . '/includes/partials/service-card.php';
```

