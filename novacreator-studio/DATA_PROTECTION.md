# 🔒 Защита данных пользователей

## Гарантии сохранности данных

### ✅ Что защищено:

1. **База данных `data/app.db`**
   - ✅ Добавлена в `.gitignore` - не попадает в Git
   - ✅ Не удаляется при `git pull`
   - ✅ Не перезаписывается при обновлении кода
   - ✅ Все данные пользователей сохраняются

2. **Данные пользователей**
   - ✅ Регистрации и профили
   - ✅ Прогресс проектов
   - ✅ Время работы
   - ✅ Метрики и статистика
   - ✅ OAuth связи

3. **OAuth конфигурация**
   - ✅ Хранится в базе данных
   - ✅ Автоматически восстанавливается из кода
   - ✅ Можно экспортировать/импортировать

## Как работает защита

### Миграции базы данных

Миграции **НЕ удаляют данные**! Они только:

- ✅ Создают таблицы, если их нет (`CREATE TABLE IF NOT EXISTS`)
- ✅ Добавляют новые колонки, если их нет (`ALTER TABLE ... ADD COLUMN`)
- ✅ Создают индексы, если их нет (`CREATE INDEX IF NOT EXISTS`)
- ❌ **НИКОГДА** не удаляют таблицы
- ❌ **НИКОГДА** не удаляют колонки
- ❌ **НИКОГДА** не удаляют данные

### При обновлении через Git

```bash
git pull origin main
```

**Что происходит:**

1. ✅ Обновляется код (PHP, CSS, JS файлы)
2. ✅ База данных `data/app.db` остается нетронутой
3. ✅ При первом запросе применяются миграции (добавляются новые поля)
4. ✅ Все существующие данные сохраняются

## Проверка защиты данных

### Перед обновлением:

```bash
# Проверка защиты данных
php scripts/verify_data_protection.php
```

Скрипт проверит:
- ✅ База данных существует
- ✅ База данных в `.gitignore`
- ✅ База данных не отслеживается Git
- ✅ OAuth конфигурация настроена
- ✅ Есть данные пользователей

### Автоматический бэкап:

```bash
# Создать бэкап перед обновлением
./scripts/backup_before_update.sh
```

Создаст:
- ✅ Резервную копию базы данных
- ✅ Резервную копию OAuth конфигурации
- ✅ Ссылки на последние бэкапы

## Безопасное обновление

### Рекомендуемый процесс:

```bash
# 1. Проверка защиты
php scripts/verify_data_protection.php

# 2. Создание бэкапа
./scripts/backup_before_update.sh

# 3. Обновление кода
git pull origin main

# 4. Проверка после обновления
php scripts/verify_data_protection.php
```

## Восстановление данных

### Если что-то пошло не так:

```bash
# Восстановить базу данных
cp data/backups/app.db.latest data/app.db

# Восстановить OAuth конфигурацию
php scripts/import_oauth_config.php data/backups/oauth_config.latest.json
```

## Защита OAuth credentials

OAuth credentials встроены в код для гарантированной работы, но:

- ⚠️ Они видны в исходном коде
- ✅ Они автоматически устанавливаются на сервере
- ✅ Можно переопределить через переменные окружения
- ✅ Можно экспортировать/импортировать через скрипты

### Рекомендации:

1. **На продакшене:** Используйте переменные окружения в `.htaccess`
2. **Регулярно:** Обновляйте OAuth credentials
3. **Бэкапы:** Делайте резервные копии перед обновлениями

## Что НЕ будет потеряно при обновлении

✅ **Все данные пользователей:**
- Имена, email, пароли
- OAuth связи
- Аватары

✅ **Все данные проектов:**
- Прогресс (%)
- Время работы (минуты)
- Статусы и этапы
- Заметки
- Даты начала и обновления
- Метрики (средний прогресс, часы, дни до завершения)

✅ **OAuth конфигурация:**
- Google OAuth credentials
- Apple OAuth credentials (если настроены)

## Что обновится

✅ **Код:**
- PHP файлы
- CSS/JS файлы
- HTML шаблоны
- Конфигурационные файлы

✅ **Структура БД:**
- Новые таблицы (если добавлены)
- Новые колонки (если добавлены)
- Новые индексы (если добавлены)

❌ **НЕ обновится:**
- Существующие данные
- Существующие таблицы
- Существующие колонки

## Итог

**Ваши данные в безопасности!** 🛡️

- База данных защищена от Git
- Миграции не удаляют данные
- Автоматические бэкапы доступны
- Восстановление из бэкапов возможно

Можно безопасно обновлять код через Git без потери данных пользователей.

