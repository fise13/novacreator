# 🚀 Инструкция по деплою на хостинг

## Подготовка к деплою

### 1. Скомпилируйте CSS

```bash
cd novacreator-studio
npm install
npm run build-css-prod
```

### 2. Настройте конфигурацию

1. Откройте `novacreator-studio/telegram_bot/config.php`
2. Укажите токен бота и Chat ID
3. Откройте `novacreator-studio/includes/seo_meta.php`
4. Замените домен на ваш реальный (строка 8-9)

### 3. Загрузка на хостинг

**Вариант 1: Через FTP/SFTP**
1. Подключитесь к хостингу через FTP
2. Загрузите **всё содержимое** папки `novacreator-studio/` в папку `httpdocs/` (или другую корневую папку сайта)
3. Убедитесь, что структура сохранилась

**Вариант 2: Через Git (если поддерживается)**
1. На хостинге настройте Git репозиторий
2. Клонируйте репозиторий в папку сайта
3. Скопируйте содержимое `novacreator-studio/` в корневую папку сайта

**Вариант 3: Через панель управления хостингом**
1. Заархивируйте папку `novacreator-studio/`
2. Загрузите архив через панель управления
3. Распакуйте архив в корневую папку сайта

## Важные настройки после деплоя

### 1. Права доступа

Установите права на папки:
```bash
chmod 755 novacreator-studio/
chmod 644 novacreator-studio/*.php
chmod 777 novacreator-studio/backend/  # Для записи requests.txt
```

### 2. Обновите домен в файлах

После загрузки обновите домен в:
- `robots.txt` - строка с Sitemap
- `sitemap.xml` - все URL
- `includes/seo_meta.php` - базовый URL

### 3. Проверьте работу

1. Откройте сайт в браузере
2. Проверьте отправку формы
3. Проверьте работу Telegram бота
4. Проверьте robots.txt: `ваш-домен.com/robots.txt`
5. Проверьте sitemap.xml: `ваш-домен.com/sitemap.xml`

## Структура на хостинге

После загрузки структура должна быть такой:

```
httpdocs/  (или ваша корневая папка)
├── index.php
├── about.php
├── services.php
├── ... (все PHP файлы)
├── assets/
├── backend/
├── includes/
├── telegram_bot/
├── robots.txt
├── sitemap.xml
└── .htaccess
```

## Настройка SSL

После получения SSL сертификата:
1. Обновите `.htaccess` - раскомментируйте редирект на HTTPS
2. Обновите все URL в `sitemap.xml` и `seo_meta.php` на `https://`

## Проверка SEO

После деплоя:
1. Отправьте sitemap.xml в Google Search Console
2. Отправьте sitemap.xml в Яндекс.Вебмастер
3. Проверьте индексацию через несколько дней

