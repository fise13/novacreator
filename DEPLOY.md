# 🚀 Инструкция по деплою на хостинг

## Подготовка к загрузке на хостинг

### 1. Структура файлов на хостинге

Все файлы должны быть в папке `httpdocs` (или `public_html` в зависимости от хостинга):

```
httpdocs/
├── index.php
├── services.php
├── seo.php
├── ads.php
├── portfolio.php
├── about.php
├── contact.php
├── vacancies.php
├── 404.php
├── 500.php
├── robots.txt
├── sitemap.xml
├── .htaccess
├── assets/
│   ├── css/
│   │   ├── input.css
│   │   └── output.css
│   ├── js/
│   │   └── main.js
│   └── img/
├── backend/
│   └── send.php
├── includes/
│   ├── header.php
│   ├── footer.php
│   ├── seo_meta.php
│   ├── breadcrumbs.php
│   └── paths.php
└── telegram_bot/
    ├── config.php (создайте на хостинге)
    ├── send_telegram.php
    └── ...
```

### 2. Загрузка файлов

**Вариант 1: Через FTP**
1. Подключитесь к FTP хостинга
2. Загрузите все файлы в папку `httpdocs`
3. Убедитесь, что структура папок сохранена

**Вариант 2: Через Git (если поддерживается)**
1. На хостинге в папке `httpdocs` выполните:
   ```bash
   git clone https://github.com/fise13/novacreator.git .
   ```
2. Или если репозиторий уже есть:
   ```bash
   git pull origin main
   ```

### 3. Настройка на хостинге

#### Создайте config.php для Telegram бота:
```bash
cd httpdocs/telegram_bot
cp config.example.php config.php
# Отредактируйте config.php и добавьте токен и Chat ID
```

#### Установите права доступа:
```bash
chmod 755 httpdocs
chmod 644 httpdocs/*.php
chmod 755 httpdocs/backend
chmod 644 httpdocs/backend/send.php
chmod 777 httpdocs/backend  # Для записи requests.txt
```

### 4. Обновите домен в файлах

Откройте и обновите домен в следующих файлах:

1. **robots.txt** - раскомментируйте и укажите ваш домен:
   ```
   Sitemap: https://novacreatorstudio.com/sitemap.xml
   ```

2. **includes/seo_meta.php** - домен определяется автоматически, но можно указать явно:
   ```php
   $siteUrl = 'https://novacreatorstudio.com';
   ```

3. **sitemap.xml** - замените все упоминания домена на ваш

### 5. Проверка работы

1. Откройте сайт: `https://novacreatorstudio.com`
2. Проверьте, что CSS и JS загружаются
3. Проверьте форму обратной связи
4. Проверьте работу Telegram бота

### 6. Настройка SSL (важно!)

На скриншоте видно предупреждение "Домен не защищен". Нужно:

1. В панели хостинга найдите "SSL/TLS-сертификаты"
2. Установите SSL сертификат (Let's Encrypt - бесплатный)
3. Включите принудительный редирект на HTTPS в .htaccess

### 7. Обновление .htaccess для HTTPS

Раскомментируйте в `.htaccess`:
```apache
RewriteCond %{HTTPS} off
RewriteRule ^(.*)$ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]
```

### 8. Отправка sitemap в поисковые системы

После запуска:
1. Google Search Console: добавьте sitemap `https://novacreatorstudio.com/sitemap.xml`
2. Яндекс.Вебмастер: добавьте sitemap `https://novacreatorstudio.com/sitemap.xml`

## ⚠️ Важные моменты

- **Пути автоматически определяются** - функция `asset()` и `url()` работают на любом хостинге
- **Конфиденциальные файлы** - `telegram_bot/config.php` не должен быть в Git, создайте его на хостинге
- **Права доступа** - убедитесь, что PHP может писать в `backend/` для сохранения заявок
- **PHP версия** - на хостинге PHP 8.2.29, все должно работать

## 🔧 Решение проблем

### Если CSS/JS не загружаются:
- Проверьте пути в браузере (F12 → Network)
- Убедитесь, что файлы загружены в правильные папки
- Проверьте права доступа к файлам

### Если формы не работают:
- Проверьте путь к `backend/send.php` в консоли браузера
- Убедитесь, что PHP может выполнять скрипты
- Проверьте логи ошибок на хостинге

### Если Telegram не работает:
- Проверьте, что `telegram_bot/config.php` создан и настроен
- Убедитесь, что на хостинге разрешены исходящие HTTPS запросы
- Проверьте логи в `backend/telegram_errors.log`

