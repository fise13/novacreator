# Быстрый старт

## 1. Перейдите в папку сайта

```bash
cd novacreator-studio
```

## 2. Установка зависимостей

```bash
npm install
```

## 3. Компиляция CSS

```bash
npm run build-css-prod
```

## 4. Запуск локального сервера

```bash
php -S localhost:8000
```

## 5. Откройте в браузере

```
http://localhost:8000
```

## 5. Настройка email (опционально)

Откройте `backend/send.php` и измените:

```php
$emailTo = 'your-email@example.com';
```

**Важно**: Для работы email на сервере должна быть настроена почта. 
В продакшене рекомендуется использовать PHPMailer.

## Готово! 🎉

Сайт готов к использованию. Все формы будут сохраняться в `backend/requests.txt`.

## Для разработки

Если вы хотите автоматическую перекомпиляцию CSS при изменениях:

```bash
npm run build-css
```

Оставьте эту команду запущенной в отдельном терминале.

